import { MethodeNamePipe } from './methode-name.pipe';

describe('MethodeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MethodeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
